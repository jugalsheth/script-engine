# VIBE CODING — seed the active series. One topic per line.
# Clear or replace when the series rotates. Comments (#) are ignored.
Claude Code + Higgsfield MCP: generate the product shot while the agent writes the page
The Cursor setting that stopped my token overspend
CLAUDE.md rules that actually make Claude Code ship instead of wander
One MCP mashup every builder should wire this week
I vibe coded for 3 hours and shipped nothing — here's the fix
